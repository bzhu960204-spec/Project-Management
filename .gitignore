# ── Maven build output ────────────────────────────────────────────
target/

# ── Frontend ──────────────────────────────────────────────────────
node_modules/
dist/

# ── Runtime logs ──────────────────────────────────────────────────
logs/
*.log

# ── H2 database files (runtime data lives in OneDrive via junction) ─
# backend/data is a junction to OneDrive; never commit the database.
backend/data/
backend/data/*.db
backend/data/*.mv.db
backend/data/*.trace.db
backend/data/*.lock.db

# ── Dev state ────────────────────────────────────────────────────
.pm-dev-state.json

# ── Machine-local config ─────────────────────────────────────────
# See README — Java override for this PM app itself.
.java-home

# ── IDE / editor ─────────────────────────────────────────────────
.idea/
*.iml
.vscode/

